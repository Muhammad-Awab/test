python -c "
import glob, json, os
files = glob.glob(os.path.expanduser('~/.aws/sso/cache/*.json'))
for f in files:
    try:
        d = json.load(open(f))
        if 'accessToken' in d:
            print('Token found:', f)
            print(d['accessToken'][:20], '...')
    except:
        pass
"
